Install with npm:

```console
npm install @yarunoka/builder @yarunoka/core
```

Requires Node.js 26 or newer — or any runtime with the Temporal API,
which a polyfill can supply. `@yarunoka/core` is a peer dependency.
ESM only.
